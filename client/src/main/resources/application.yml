server:
  port: 8010
spring:
  thymeleaf:
    prefix: classpath:/static/
    suffix: .html
  application:
    name: client
  jackson:
    default-property-inclusion: non_null
  resources:
    static-locations: classpath:/static,file:D:\Desktop\pictures
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true